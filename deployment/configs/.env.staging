# MAGSASA-CARD Platform - Staging Environment Configuration
# Render Deployment Environment Variables

# Application Configuration
ENVIRONMENT=staging
FLASK_ENV=production
FLASK_DEBUG=false
SECRET_KEY=staging-secret-key-change-in-production

# Database Configuration
DATABASE_TYPE=sqlite
DATABASE_PATH=/opt/render/project/src/database/dynamic_pricing.db

# API Configuration
ALLOWED_ORIGINS=*
API_VERSION=2.1.0

# AI Service Configuration (Set these in Render dashboard)
# OPENAI_API_KEY=your_openai_api_key_here
# GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=json

# Performance Configuration
WORKERS=2
TIMEOUT=120
MAX_REQUESTS=1000
MAX_REQUESTS_JITTER=100

# Feature Flags
KAANI_INTEGRATION=true
AI_DIAGNOSIS=true
AGSCORE_ASSESSMENT=true
AB_TESTING=true
ADVANCED_ANALYTICS=false

# Monitoring Configuration
HEALTH_CHECK_ENABLED=true
METRICS_ENABLED=true
REQUEST_LOGGING=true

# Security Configuration
SSL_REQUIRED=true
CORS_ENABLED=true

# Cache Configuration (if using Redis in future)
# REDIS_URL=redis://localhost:6379/0
# CACHE_TTL=3600

# External Service URLs
# PAGASA_API_URL=https://api.pagasa.dost.gov.ph
# CARD_MRI_API_URL=https://api.card-mri.org

# Notification Configuration (for future use)
# NOTIFICATION_EMAIL=admin@magsasa-card.com
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_FREQUENCY=daily

# Rate Limiting (if implemented)
RATE_LIMIT_ENABLED=false
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600

# Development/Testing Configuration
TESTING_MODE=false
MOCK_AI_RESPONSES=false

# Deployment Metadata
DEPLOYMENT_TIMESTAMP=2025-09-28T12:00:00Z
DEPLOYMENT_VERSION=2.1.0
DEPLOYMENT_PLATFORM=render

# Notes:
# 1. Set sensitive values (API keys) directly in Render dashboard
# 2. This file is for reference and local testing
# 3. Do not commit actual API keys to version control
# 4. Update DEPLOYMENT_TIMESTAMP when deploying
# 5. Staging uses SQLite for simplicity
# 6. Production will use PostgreSQL
